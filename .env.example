# Apps Script 웹앱의 배포 URL (읽기 전용).
# 비워 두면 앱은 public/demo-data.json의 예시 데이터를 보여주고,
# 설정 화면에서 URL을 직접 넣을 수 있다.
#
# 이 값은 공개되어도 된다 (§13.3): 앱을 여는 사람의 브라우저에 어차피 노출되고,
# services 시트는 참석을 파트별 숫자로만 기록하므로 개인정보가 없다.
VITE_API_URL=https://script.google.com/macros/s/AKfycb.../exec
